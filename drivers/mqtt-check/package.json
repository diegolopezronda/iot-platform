{
  "name": "dr-mqtt-check",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm install;pm2 start app.js --name dr-mqtt-check --merge-logs -o logs/out.log -e logs/error.log",
    "restart": "pm2 restart dr-mqtt-check;pm2 logs dr-mqtt-check",
    "log": "pm2 logs dr-mqtt-check",
    "errors": "pm2 logs --err dr-mqtt-check",
    "clean-errors": "rm logs/err.log"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "moment": "^2.18.1",
    "mongodb": "^2.2.31",
    "mqtt": "^2.12.1"
  }
}
